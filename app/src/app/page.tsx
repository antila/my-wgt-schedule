// 'use client'

// import { getData, useDataQuery } from '../hooks/dataHook'

// export async function getServerSideProps() {
//   const initialData = await getData()
//   return { props: { initialData } }
// }

export default function Home() {
  return <div>Home</div>
}
